package ru.mareanexx.ui.theme

import androidx.compose.ui.graphics.Color

// Общее

val titleMain = Color(0xFF353269)
val bodySecondary = Color(0xFF5A55D7)

val backgroundMain = Color(0xFFEEEDFD)
val onBackgroundMain = Color(0xFFF0F0F7)
val outerShadow = Color(0x3369688B)


val backgroundSecondary = Color(0xFFFBFAFE)

// Навигейшн бар

val navBackground = Color(0xFFFBFAFE)
val selectedTab = Color(0xFF5A55D7)
val deselectedTab = Color(0xFF706CAC)

// Экран Главная

// Моя библиотека

// Мои коллекции

val bigCardBackground = Color(0xFF7970FF)
val favoriteStar = Color(0xFFFFD67D)

val cardBookCounter = Color(0xFF8F8EA5)

// Мой профиль

val username = Color(0xFF727093)
val divider = Color(0x80727093)
val notesIcon = Color(0xFF8D8AB4)
val descriptionBackground = Color(0xFFD9D7EF)

// О книге

val authorsText = Color(0xFF727093)
val tagVioletBackground = Color(0xFFDEDBFC)
val tagRedBackground = Color(0xFFEECCCD)
val tagRedText = Color(0xFFD75557)

val addToWantToReadBackground = Color(0xFFDEDBFC)
val rating = Color(0xFFFCA930)
val ratingTrackColor = Color(0xFFE1DFF0)

// Чтение

